# README_EXAMPLES

## Summary

Update README.md to document how to import and use the library and demonstrate expected outputs for the primary API. Documentation should make the library immediately usable for a developer who clones the repository.

## Specification

- Add a short usage section showing how to import the named exports from the module entry point and how to call fizzBuzz and fizzBuzzSingle.
- Show example invocations and their expected results in prose, including fizzBuzzSingle(3) -> Fizz and fizzBuzz(15) ending with FizzBuzz, without embedding runnable scripts that depend on external tools.
- Mention the npm test command and that tests verify both normal behaviour and input validation rules.

## Acceptance criteria

- README.md contains a Usage section that names the exported functions and describes their behaviour.
- README.md includes at least two example calls and their expected outputs described in plain text.
- README.md references running npm test to validate behaviour.
