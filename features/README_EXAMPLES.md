# README_EXAMPLES

Summary

Update README.md to include usage examples showing how to import and call both fizzBuzz and fizzBuzzSingle, and example outputs for common inputs.

Specification

- README must demonstrate importing named exports from src/lib/main.js and show example results for fizzBuzzSingle(3), fizzBuzzSingle(5), fizzBuzz(15), and fizzBuzz(0).
- Examples should be short, copy-pastable snippets and show expected string or array outputs.

Acceptance Criteria

- [ ] README.md includes examples showing fizzBuzzSingle(3) => Fizz, fizzBuzzSingle(5) => Buzz, fizzBuzzSingle(15) => FizzBuzz.
- [ ] README.md shows that fizzBuzz(15) returns an array of 15 strings and that fizzBuzz(0) returns an empty array.
- [ ] README.md points to the tests and the CLI usage for quick verification.
