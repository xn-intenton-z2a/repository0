# FIZZBUZZ_TYPES

DEPRECATED: This feature has been consolidated into README-focused documentation and the TypeScript declarations task will be tracked separately if requested. Keep this file for historical reference; do not implement new work here.

Notes

- If TypeScript declarations are required in future, create src/lib/main.d.ts and add a types entry in package.json pointing to that file.
- Tests for declarations should be lightweight filesystem checks; avoid invoking the TypeScript compiler in CI for speed.
