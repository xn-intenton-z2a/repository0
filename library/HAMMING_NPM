TABLE OF CONTENTS
1. Purpose of document
2. Crawl result and retrieval status
3. Recommended retrieval (registry) and exact metadata fields
4. Suggested package usage patterns (extraction guidance)
5. Detailed digest
6. Attribution and size

1. Purpose of document
Record of the npm package source listed in SOURCES.md and precise instructions to extract authoritative package metadata and API from npm registry when website content is blocked by bot protection.

2. Crawl result and retrieval status
- Seed URL: https://www.npmjs.com/package/hamming-distance
- HTTP fetch returned a Cloudflare-managed JavaScript challenge page ("Just a moment...") which blocks automated scraping; direct HTML payload contains challenge orchestration rather than package details.
- Because the web UI enforces JS and challenge flows, automated extraction must use the npm registry API instead of the public web page.

3. Recommended retrieval (registry) and exact metadata fields
- Use registry endpoint: https://registry.npmjs.org/hamming-distance (returns package metadata JSON)
- Important fields to extract from registry JSON:
  - name, version, description
  - dist-tags.latest
  - versions[<resolved>] -> main entry point (module/main), scripts, dependencies, files
  - readme (full README markdown)
  - repository.url
  - license
  - maintainers
- If README is large, extract specific sections: API, Usage, Examples, Exported function signatures.

4. Suggested package usage patterns (extraction guidance)
- Look for exported functions in the package's main/module entry; typical patterns for a hamming-distance package:
  - function hamming(a: string|Buffer|Uint8Array, b: string|Buffer|Uint8Array): number
  - Input types supported (buffers, strings) should be detected from README or package source under versions[].
- If the package has no repository or source files in versions entry, fetch tarball at versions[ver].dist.tarball and inspect package.json and index files.

5. Detailed digest
- Retrieval attempt: 2026-03-14 — public web UI blocked by bot protection; content is a Cloudflare challenge page instead of package metadata.
- Actionable next step: curl https://registry.npmjs.org/hamming-distance to obtain canonical JSON metadata, then inspect versions.latest and tarball.

6. Attribution and size
- Attribution: npmjs.com — package page (web UI blocked); registry API recommended for machine extraction.
- Crawled approximate data size (challenge page): small HTML payload returned (challenge orchestration), not authoritative package data; use registry instead.
