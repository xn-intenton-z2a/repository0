Sources for hamming distance implementation and tests.

- Implemented according to repository Mission and issue requirements.
- Uses Unicode code point iteration via Array.from to correctly compare characters that span surrogate pairs.
- Uses BigInt normalization for reliable bitwise operations on large integers.
