Usage:

Require and call the fizzbuzz function from src/web/lib-browser.js

Example:
const fizzbuzz = require('../../src/web/lib-browser');
console.log(fizzbuzz(15)); // -> "FizzBuzz"

Running tests:
- Install deps (if any)
- Run your test runner (e.g., mocha or npm test) to execute tests in tests/unit
