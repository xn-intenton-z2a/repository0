{
  "name": "plot-code-lib",
  "version": "0.1.0",
  "description": "Mathematical plotting library and CLI tool - be the jq of formulae visualisations",
  "type": "module",
  "main": "src/lib/main.js",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "node .github/agentic-lib/scripts/build-web.cjs",
    "test": "vitest --run tests/unit/*.test.js",
    "test:unit": "vitest --run --coverage tests/unit/*.test.js",
    "test:behaviour": "npx playwright test --config playwright.config.js",
    "start": "npm run build:web && npx serve docs",
    "start:cli": "node src/lib/main.js"
  },
  "keywords": ["plotting", "mathematics", "cli", "svg", "png", "charts", "visualization", "formulae"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@xn-intenton-z2a/agentic-lib": "^7.4.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^26.1.0",
    "serve": "^14.2.4",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}