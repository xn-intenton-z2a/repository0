{
  "name": "repo",
  "version": "0.1.0",
  "description": "Roman numeral conversion library demo",
  "type": "module",
  "main": "src/lib/main.js",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "node -e \"const fs=require('fs');const pkg=require('./package.json');fs.mkdirSync('docs',{recursive:true});fs.writeFileSync('docs/.nojekyll','');fs.copyFileSync('src/web/index.html','docs/index.html');fs.writeFileSync('docs/lib-meta.js','export const name = '+JSON.stringify(require('./package.json').name)+';\\nexport const version = '+JSON.stringify(require('./package.json').version)+';\\nexport const description = '+JSON.stringify(require('./package.json').description)+';\\n');fs.writeFileSync('docs/lib-main.js',`export const name = ${JSON.stringify(require('./package.json').name)};export const version = ${JSON.stringify(require('./package.json').version)};export const description = ${JSON.stringify(require('./package.json').description)};export function toRoman(n){if(typeof n!=='number'||!Number.isInteger(n))throw new TypeError('toRoman: integer');if(n<1||n>3999)throw new RangeError('toRoman');const map=[[1000,'M'],[900,'CM'],[500,'D'],[400,'CD'],[100,'C'],[90,'XC'],[50,'L'],[40,'XL'],[10,'X'],[9,'IX'],[5,'V'],[4,'IV'],[1,'I']];let r='';let rem=n;for(const [v,s] of map){while(rem>=v){r+=s;rem-=v}}return r}export function fromRoman(str){if(typeof str!=='string')throw new TypeError('fromRoman');const s=str.trim().toUpperCase();if(!/^M{0,3}(CM|CD|D?C{0,3})(XC|XL|L?X{0,3})(IX|IV|V?I{0,3})$/.test(s))throw new TypeError('fromRoman');const vals={I:1,V:5,X:10,L:50,C:100,D:500,M:1000};let total=0;for(let i=0;i<s.length;i++){const cur=vals[s[i]];const next=vals[s[i+1]]||0;if(cur<next){total+=next-cur;i++}else total+=cur}return total}`);console.log('built docs');\"",
    "test": "vitest --run tests/unit/*.test.js",
    "test:unit": "vitest --run --coverage tests/unit/*.test.js",
    "test:behaviour": "npm run build:web && npx playwright test --config playwright.config.js",
    "start": "npm run build:web && npx serve docs",
    "start:cli": "node src/lib/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@xn-intenton-z2a/agentic-lib": "^7.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^26.1.0",
    "serve": "^14.2.4",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
