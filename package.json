{
  "name": "@xn-intenton-z2a/repository0",
  "version": "1.4.1-5",
  "description": "Demo repository showcasing agentic‑lib workflows with a minimal CLI demonstration. Updated to standardize error messaging for arithmetic operations, add JSON output mode with metadata, include a configurable warning index mode, configurable punctuation stripping via TOKEN_PUNCTUATION_CONFIG, suppression of NaN correction suggestions, added --toggle-allow-nan for runtime NaN configuration, and new --allow-nan-inline flag for per-command NaN acceptance.",
  "type": "module",
  "main": "src/lib/main.js",
  "scripts": {
    "build": "echo 'Nothing to build'",
    "formatting": "prettier --check .",
    "formatting-fix": "prettier --write .",
    "linting": "eslint",
    "linting-json": "eslint --format=@microsoft/eslint-formatter-sarif",
    "linting-fix": "eslint --fix",
    "update-to-minor": "npx ncu --upgrade --enginesNode --target minor --verbose --install always",
    "update-to-greatest": "npx ncu --upgrade --enginesNode --target greatest --verbose --install always --reject 'alpha'",
    "test": "vitest",
    "test:unit": "vitest --coverage",
    "coverage": "vitest --coverage",
    "start": "node src/lib/main.js",
    "diagnostics": "node src/lib/main.js --diagnostics",
    "version": "node src/lib/main.js --version"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "dotenv": "^16.4.7",
    "openai": "^4.93.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@microsoft/eslint-formatter-sarif": "^3.1.0",
    "@vitest/coverage-v8": "^3.1.1",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-import": "^2.31.0",
    "eslint-plugin-prettier": "^5.2.6",
    "eslint-plugin-promise": "^7.2.1",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-security": "^3.0.1",
    "eslint-plugin-sonarjs": "^3.0.2",
    "eslint": "^9.24.0",
    "markdown-it": "^14.1.0",
    "markdown-it-github": "^0.5.0",
    "npm-check-updates": "^17.1.16",
    "openai": "^4.93.0",
    "prettier": "^3.5.3",
    "vitest": "^3.0.9"
  },
  "overrides": {
    "rimraf": "^4.0.0",
    "glob": "^9.3.0",
    "@humanwhocodes/config-array": "^0.13.0",
    "@humanwhocodes/object-schema": "^2.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "package.json"
  ],
  "eslintIgnore": [
    "archive/**",
    "exports/**",
    "build/**",
    "coverage/**",
    "dist/**",
    "node_modules/**"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
